import { CanEditDirective } from './can-edit.directive';

describe('CanEditDirective', () => {
  it('should create an instance', () => {
    const directive = new CanEditDirective();
    expect(directive).toBeTruthy();
  });
});
