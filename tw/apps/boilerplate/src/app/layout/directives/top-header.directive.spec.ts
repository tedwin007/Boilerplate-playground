import { TopHeaderDirective } from './top-header.directive';

describe('TopHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new TopHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
