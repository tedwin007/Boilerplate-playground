import { SubHeaderDirective } from './sub-header.directive';

describe('SubHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new SubHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
