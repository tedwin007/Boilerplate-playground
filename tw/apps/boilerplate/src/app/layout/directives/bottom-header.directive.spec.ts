import { BottomHeaderDirective } from './bottom-header.directive';

describe('BottomHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new BottomHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
